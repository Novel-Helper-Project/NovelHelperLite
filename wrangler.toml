# wrangler.toml - Cloudflare Pages 配置
name = "novel-helper-lite"
compatibility_date = "2025-11-12"

# Pages 配置 - 指定构建输出目录
pages_build_output_dir = "dist/spa"

# 构建配置（可选 - Cloudflare Pages 会使用项目的构建命令）
# 构建命令：npm run build
# 输出目录：dist/spa

# 环境变量
[vars]
# 添加环境变量